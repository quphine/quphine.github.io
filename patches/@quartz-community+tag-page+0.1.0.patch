diff --git a/node_modules/@quartz-community/tag-page/dist/components/index.js b/node_modules/@quartz-community/tag-page/dist/components/index.js
index c58e177..5743826 100644
--- a/node_modules/@quartz-community/tag-page/dist/components/index.js
+++ b/node_modules/@quartz-community/tag-page/dist/components/index.js
@@ -1,3 +1,41 @@
+import katex from "katex";
+
+function escapeHtml(str) {
+  return str
+    .replace(/&/g, "&amp;")
+    .replace(/</g, "&lt;")
+    .replace(/>/g, "&gt;")
+    .replace(/"/g, "&quot;")
+    .replace(/'/g, "&#39;");
+}
+
+function renderMathTitle(raw) {
+  if (!raw) return "";
+  const mathPattern = /\$\$([^$]+?)\$\$|\$([^$]+?)\$/g;
+  let result = "";
+  let lastIndex = 0;
+  let match;
+  while ((match = mathPattern.exec(raw)) !== null) {
+    result += escapeHtml(raw.slice(lastIndex, match.index));
+    const displayMath = match[1];
+    const inlineMath = match[2];
+    const isDisplay = displayMath !== undefined;
+    const expr = isDisplay ? displayMath : inlineMath;
+    try {
+      result += katex.renderToString(expr ?? "", {
+        throwOnError: false,
+        displayMode: false,
+        output: "html"
+      });
+    } catch (err) {
+      result += escapeHtml(match[0]);
+    }
+    lastIndex = mathPattern.lastIndex;
+  }
+  result += escapeHtml(raw.slice(lastIndex));
+  return result;
+}
+
 var __create = Object.create;
 var __defProp = Object.defineProperty;
 var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
@@ -442,7 +480,7 @@ var PageList = ({
         {
           href: resolveRelative(fileSlug ?? "", page.slug),
           class: "internal",
-          children: title
+          dangerouslySetInnerHTML: { __html: renderMathTitle(title) }
         }
       ) }) }),
       /* @__PURE__ */ u2("ul", { class: "tags", children: tags.map((tag) => /* @__PURE__ */ u2("li", { children: /* @__PURE__ */ u2(
diff --git a/node_modules/@quartz-community/tag-page/dist/index.js b/node_modules/@quartz-community/tag-page/dist/index.js
index 9a9ecda..4428d0b 100644
--- a/node_modules/@quartz-community/tag-page/dist/index.js
+++ b/node_modules/@quartz-community/tag-page/dist/index.js
@@ -1,3 +1,41 @@
+import katex from "katex";
+
+function escapeHtml(str) {
+  return str
+    .replace(/&/g, "&amp;")
+    .replace(/</g, "&lt;")
+    .replace(/>/g, "&gt;")
+    .replace(/"/g, "&quot;")
+    .replace(/'/g, "&#39;");
+}
+
+function renderMathTitle(raw) {
+  if (!raw) return "";
+  const mathPattern = /\$\$([^$]+?)\$\$|\$([^$]+?)\$/g;
+  let result = "";
+  let lastIndex = 0;
+  let match;
+  while ((match = mathPattern.exec(raw)) !== null) {
+    result += escapeHtml(raw.slice(lastIndex, match.index));
+    const displayMath = match[1];
+    const inlineMath = match[2];
+    const isDisplay = displayMath !== undefined;
+    const expr = isDisplay ? displayMath : inlineMath;
+    try {
+      result += katex.renderToString(expr ?? "", {
+        throwOnError: false,
+        displayMode: false,
+        output: "html"
+      });
+    } catch (err) {
+      result += escapeHtml(match[0]);
+    }
+    lastIndex = mathPattern.lastIndex;
+  }
+  result += escapeHtml(raw.slice(lastIndex));
+  return result;
+}
+
 var __create = Object.create;
 var __defProp = Object.defineProperty;
 var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
@@ -426,7 +464,7 @@ var PageList = ({
         {
           href: resolveRelative(fileSlug ?? "", page.slug),
           class: "internal",
-          children: title
+          dangerouslySetInnerHTML: { __html: renderMathTitle(title) }
         }
       ) }) }),
       /* @__PURE__ */ u2("ul", { class: "tags", children: tags.map((tag) => /* @__PURE__ */ u2("li", { children: /* @__PURE__ */ u2(
